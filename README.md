# 手机扫码支付 PRD 网页

这是一个可直接用于线上部署的静态前端页面，用于展示“手机扫码支付”PRD 文档和低保真原型图。

## 本地运行

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

## Netlify 部署

- Build command: `npm run build`
- Publish directory: `dist`

项目已包含 `netlify.toml`，Netlify 可自动读取。
